using System;

namespace App
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
    //NetworkCredential NCredentials = new NetworkCredential(TextBox1.Text, TextBox2.Text);

    //    using (new NetworkConnection(@"\\server\sharedfolder", NCredentials))
    //    {
            
    //        string sDir = @"\\server\sharedfolder";
    //        foreach (string d in Directory.GetFiles(sDir))
    //        {

    //            Label1.Text = Label1.Text + d + "</br>";

    //        }
          
    //    }
}
